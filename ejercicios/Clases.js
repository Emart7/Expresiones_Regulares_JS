// Repara que tenemos solamente un match. La regex engine aplicó el carácter a o +, 
// una o más veces en la string.

// Ahora que sabes de eso y dada la siguiente string: ?clases+poderosas*

// ¿Qué regex abajo selecciona toda la string (un match solamente)?


// [a-z?+*]+